{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The minimum the Herald window needs: open a link, check for an update, and relaunch into it.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "updater:default",
    "process:allow-restart"
  ]
}
