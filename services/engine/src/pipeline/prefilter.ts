/** Moved to @herald/core so the phone applies the same rules. See pipeline/dedupe.ts. */
export type { PrefilterResult } from '@herald/core';
export { prefilter, matchesAnyRole } from '@herald/core';
